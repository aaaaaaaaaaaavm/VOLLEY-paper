# The core environment. Everything the gates need and everything analysis/ imports.
# Pinned to what the published results were produced with, so a re-run that disagrees is a
# finding rather than a version difference.
numpy==2.4.6
scipy==1.17.1
matplotlib==3.11.1
magpylib==5.2.3

# A67's global sensitivity analysis. Sobol indices over the guided-contact model.
SALib==1.5.2

# Real-gas properties, for checking the closed-expansion model's ideal-gas assumption.
CoolProp==8.0.0

# The test harness and the independent-check tooling are in requirements-dev.txt beside this
# file. Nothing in it produces a number that appears in this repository, so it is kept apart from
# the packages above, which do. The flagship's verify_all.sh and CI install both. In a generated
# companion the payload is here and the harness that checks it runs in the flagship.

# --- optional, and separated on purpose ---------------------------------------------------
# CAD generation only (cad/build_gen5.py, cad/build_gen6.py). NOT required by anything in
# analysis/ or by any number in the repository -- install it only to regenerate the STEP files.
# It pulls a large OCC stack, which is why it is optional rather than a hard dependency.
# See ADR-026.
#     pip install cadquery==2.8.0
#
# Meshing for the finite-element validation runs under validation/:
#     pip install gmsh==4.15.2 scikit-fem
#
# System solvers, installed outside pip: CalculiX (ccx), GetDP, ngspice, GMAT R2022a.
# tools/env-setup.sh is the record of how each was obtained.
